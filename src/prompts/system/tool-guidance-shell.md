---
name: tool-guidance-shell
description: Shell tool usage for modes that can run a terminal
---

Shell: run_terminal only (2-6 word description). `/srv` outside jail; localhost needs sandbox net; wait for unsandboxed. /Items auth: after elevation read /var/lib,/etc,~/.config. No unauth REST loop or `|| echo`.
